import 'package:flutter/material.dart';

class BotaoRedondoControladaPage extends StatelessWidget {

  const BotaoRedondoControladaPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}