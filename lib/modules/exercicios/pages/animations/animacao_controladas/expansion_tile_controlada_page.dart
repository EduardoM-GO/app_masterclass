import 'package:flutter/material.dart';

class ExpansionTileControladaPage extends StatelessWidget {

  const ExpansionTileControladaPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}